{
  "name": "dodorithmic",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen": "node scripts/generate-assets.mjs",
    "gen:overwrite": "node scripts/generate-assets.mjs --overwrite"
  },
  "devDependencies": {
    "vite": "^8.0.10"
  },
  "dependencies": {
    "phaser": "^4.1.0"
  }
}
